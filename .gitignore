# compiled binary (built to repo root by `go build ./cmd/smoked`)
/smoked

# go test/build artifacts
*.exe
*.test
*.out

# go workspace
go.work
go.work.sum

# local compose env (holds DOMAIN/ACME_EMAIL/admin password); .env.example is tracked
/.env

# real SmokePing fixture for manual `smoked import smokeping` verification (multi-MB zip;
# never committed — see cmd/smoked/import.go and CHANGELOG "SmokePing config import")
/import-test/

# local review / agent tooling artifacts (not part of the project)
/CODE_REVIEW.md
/.playwright-mcp/

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# npm deps for the browser layout test (playwright); package-lock.json is tracked
/node_modules/
