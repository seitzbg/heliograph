# compiled binary (built to repo root by `go build ./cmd/smoked`)
/smoked

# go test/build artifacts
*.exe
*.test
*.out

# go workspace
go.work
go.work.sum

# local compose env (holds DOMAIN/ACME_EMAIL/admin password); .env.example is tracked
/.env

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/
